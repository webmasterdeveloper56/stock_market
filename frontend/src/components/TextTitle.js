import { Text } from "@chakra-ui/react"

export const TextTitle = ({ title }) => {
    return (
        <Text
            fontSize={'20px'}
        >
            {title}
        </Text>
    )
}